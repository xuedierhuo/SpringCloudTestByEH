server:
  port: 8888
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
#        仓库地址
          uri: https://github.com/xuedierhuo/SpringCloudTestByEH
#          仓库路径
          search-paths: respo
#          仓库分支
          default-label: master
          username:
          password:
